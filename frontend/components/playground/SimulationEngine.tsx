// SimulationEngine is a non-visual utility. The actual simulation logic is
// handled inline in the playground page via `runSimulationTick`.
// This file exists as a stub so the import resolves.

export function SimulationEngine() {
  return null;
}
